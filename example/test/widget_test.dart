import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Example app smoke test', (tester) async {
    // The example app demonstrates go_guardian features.
    // See lib/main.dart for the full implementation.
    expect(true, isTrue);
  });
}
